#include <stdio.h>

int main(int argc, char const *argv[])
{
    int age_1;
    int age_2;

    scanf("%d %d", &age_1, &age_2);

    if (age_1 > age_2)
    {
        /* print something here. */
    }
    else
    {
        if (age_1 < age_2)
        {
            /* print something here. */
        }
        else
        {
            /* print something here. */
        }
    }

    return 0;
}
